import { Container } from "../../UI";
import styles from "./Footer.module.css";
export const Footer = () => {
  return (
    <footer className={styles.footer}>
      <Container>
        footer
      </Container>
    </footer>
  );
};
